import React from 'react';
import { Text, View } from 'react-native';
import { globalStyles } from '@styles/global';

export default function Mypage() {
  return (
    <View style={globalStyles.container}>
      <Text style={globalStyles.title}>Mypage</Text>
    </View>
  );
}
